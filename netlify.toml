[build]
  # ビルドを開始する場所を変更
  base = "/docs"

  # 公開ディレクトリ
  publish = "docs"

  # ビルドコマンド
  command = "yarn build"

  # Netlify Functions のエンドポイント対象ファイルを格納するディレクトリ
  functions = "docs/api/"


# 環境変数
[build.environment]
  VARIABLE = "value"

# リダイレクト
[[redirects]]
  from = "/old-path"
  to = "/new-path"
  status = 302
  force = true
  query = {id = ":id"}
  [redirects.headers]
    X-From = "Netlify"
    X-Api-Key = "some-api-key-string"

# カスタムヘッダー
[[headers]]
  for = "/*"
  [headers.values]
  cache-control = '''
  max-age=0,
  no-cache,
  no-store,
  must-revalidate'''